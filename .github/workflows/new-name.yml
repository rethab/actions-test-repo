name: Bockworst

on: push
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo bar > foo.txt
      - uses: actions/upload-artifact@v2
        with:
          name: foo.txt
          path: foo.txt
