name: Workflow Dispatchy

on:
  push:


jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - id: read-env
        run: |
          echo ${{ env.GITHUB_SERVER_URL }}
          echo two ${{ format('{0}', env.GITHUB_SERVER_URL) }}
