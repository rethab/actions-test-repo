name: "On Main"

on:
  push

jobs:
  be:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: mv -f backend/* .
      - run: ls -R
  fe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: mv -f frontend/* .
      - run: ls -R
