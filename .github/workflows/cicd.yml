on:
  push:
  workflow_dispatch:
    inputs:
      cluster:
        type: string

jobs:
  dump_contexts_to_log:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cluster: ${{ fromJSON(inputs.cluster) }}
    steps:
      - run: echo ${{ matrix.cluster }}
