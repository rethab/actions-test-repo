on: push

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello, world

  prod:
    runs-on: ubuntu-latest
    needs: build
    environment: production
    steps:
      - run: echo hello, production

  post_prod:
    runs-on: ubuntu-latest
    needs: prod
    steps:
      - run: echo bye, prod
