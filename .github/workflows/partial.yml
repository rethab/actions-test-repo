on:
  workflow_call:

concurrency:
  group: compliance

jobs:
  compliance:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 10
  production:
    runs-on: ubuntu-latest
    environment: production
    needs: compliance
    steps:
      - run: sleep 10
