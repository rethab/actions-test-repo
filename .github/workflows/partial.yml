on:
  workflow_call:

concurrency:
  group: partial-${{ github.ref }}

jobs:
  greet:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: confirm
    steps:
      - run: echo hello
