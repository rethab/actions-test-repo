name: "On Main"

on:
  push:
  workflow_dispatch:
    inputs:
      approval:
        description: "Do you approve?"
        required: true

concurrency:
  group: ${{ github.ref }}

jobs:
  test:
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: ${{ github.ref }}
    steps:

      - run: echo start
      - run: sleep 200
      - run: echo end
